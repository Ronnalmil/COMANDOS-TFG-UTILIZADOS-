iptables -L -n -v
lsof -i     <---- ver las redes conectadas importante
netstat -tlnp
netstat -an
netstat -r
free -h
tail -f /var/log/apache2/access.log  creo

PERMISO DE ENTRADA:
iptables -A INPUT -p tcp -s 192.168.1.0/24 --dport 22 -j ACCEPT

EL RESTO BLOQUEAR:
iptables -A INPUT -p tcp --dport 22 -j DROP

ufw habilitar en ufw para restriguir en iptables:
ufw allow from 192.168.0.0/24 to any port 22

sudo ufw reset
sudo ufw default deny incoming
sudo ufw default allow outgoing

ufw allow from 192.168.1.0/24 to any port 
iptables -A ufw-user-input -s 192.168.1.0/24 -p tcp --dport 3000 -j ACCEPT
sudo ufw enable
